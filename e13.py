import os
from sys import argv

script, fir, sec, thi = argv

print(f"the script is called: {script}.")
print("first, second, third variable is:", fir, sec, thi)

promot = '>'
likes = input(promot)
lives = input(promot)
